---
# cp secrets.yml.example secrets.yml && chmod 600 secrets.yml
# secrets.yml は gitignore 済み。terraform/secret.auto.tfvars と同じ扱い

# LiteLLM が上流（api.anthropic.com）に使う鍵
litellm_anthropic_api_key: "sk-ant-..."

# agent が LiteLLM に対して使う鍵。openssl rand -hex 32 などで生成する
litellm_master_key: "sk-..."
